# Schedule 定时任务 执行计划

> 每个任务按目录划分, 同一目录下的所有变更由一个 agent 完成。按依赖顺序执行。
> **测试任务 (type=test) 是一等公民** — 必须显式创建、必须完成、必须配对。

## 上下文引用

参考设计计划: `docs/design-plans/2026-06-13-schedule.md`
参考验证策略: design-plan「测试策略」节

## 任务清单

### Task 1: mailcode/utils/ + mailcode/relay/ — 抽 call_claude [type=impl, 已手动完成]
- **涉及目录**: `mailcode/utils/`, `mailcode/relay/`
- **涉及文件**:
  - `mailcode/utils/claude_runner.py` (新建, ~35 行: CLAUDE_TIMEOUT_SECONDS 常量 + call_claude 函数)
  - `mailcode/relay/conversation_handler.py` (修改: 删除 _CLAUDE_TIMEOUT + call_claude 函数体, 改 `from mailcode.utils.claude_runner import call_claude`)
- **描述**: 把 call_claude 从 conversation_handler 抽到 utils/claude_runner, 供 Scheduler 和 ConversationHandler 共用, 避免双份实现行为漂移
- **关联测试任务**: T2

### Task 2: tests/unit/ — claude_runner 测试 + test_conversation_handler 迁移 [type=test]
- **涉及目录**: `tests/unit/`
- **涉及文件**:
  - `tests/unit/test_claude_runner.py` (新建, 7 个测试, 从 test_conversation_handler.py::TestCallClaude 迁移)
  - `tests/unit/test_conversation_handler.py` (修改: 删除 TestCallClaude 类, 把内部所有 `patch.object(ch_module, "call_claude", ...)` 改为 `patch.object(cr_module, "call_claude", ...)`, 加 `from mailcode.utils import claude_runner as cr_module`)
- **描述**: 验证 call_claude 7 种场景, 同时确保 conversation_handler 旧测试无回归
- **验证方法**:
  - `pytest tests/unit/test_claude_runner.py -v` — 期望 7 passed
  - `pytest tests/unit/test_conversation_handler.py -v` — 期望全过 (无回归)
  - `pytest tests/unit/ -q` — 期望全过
  - `ruff check mailcode/utils/claude_runner.py tests/unit/test_claude_runner.py tests/unit/test_conversation_handler.py` — 期望 All checks passed
- **关联 impl**: T1

### Task 3: mailcode/relay/ — scheduler.py 实现 [type=impl]
- **涉及目录**: `mailcode/relay/`
- **涉及文件**:
  - `mailcode/relay/scheduler.py` (新建, ~350 行)
- **描述**: 实现 ScheduleSpec / Task 数据类 (dataclass), parse_schedule / compute_next_run 纯函数 (归一化 + 计算下次触发时间), ScheduleStore 加锁 CRUD (tmp + replace 原子写, 复用 conversation_handler 模式), Scheduler(threading.Thread) 守护线程 (主循环 while not _stopped + reload + 扫到期 + trigger + join(timeout=10) 优雅退出)
- **依赖**: T1 (call_claude 必须先抽出来)
- **关联测试任务**: T4

### Task 4: tests/unit/ — scheduler 单元测试 [type=test]
- **涉及目录**: `tests/unit/`
- **涉及文件**:
  - `tests/unit/test_scheduler.py` (新建, ~250 行)
  - `tests/unit/conftest.py` (修改, 加 `schedules_path` fixture, 复用 `temp_data_dir`)
- **描述**: 覆盖 5 个测试类: TestParseSchedule (4 种 type 合法 + 缺字段抛错 + interval 负数抛错) / TestComputeNextRun (daily 跨天 + weekly 跨周 + monthly 跨月含 day=31 在 2 月跳过 + 跨年 + interval) / TestStore (加锁读不存在返空 + 写后读回一致 + 损坏 JSON 兜底 + name 唯一性 + 10 线程并发 add) / TestSchedulerLifecycle (start/stop/join + trigger_now 同步等结果) / TestTrigger (mock 时间到点调 call_claude + send_reply / enabled=false 不触发 / dry_run=True 不发邮件 / SMTP 失败 last_status=failed)
- **验证方法**:
  - `pytest tests/unit/test_scheduler.py -v` — 期望 25+ passed
  - `pytest tests/unit/ -q` — 期望全过
  - `ruff check mailcode/relay/scheduler.py tests/unit/test_scheduler.py` — 期望 All checks passed
- **关联 impl**: T3

### Task 5: mailcode/ — schedule_cli.py 7 子命令 [type=impl]
- **涉及目录**: `mailcode/`
- **涉及文件**:
  - `mailcode/schedule_cli.py` (新建, ~350 行)
- **描述**: 仿 session_cli.py 风格, 7 个子命令函数 (cmd_schedule_list / cmd_schedule_show / cmd_schedule_add / cmd_schedule_enable / cmd_schedule_disable / cmd_schedule_delete / cmd_schedule_run_now / cmd_schedule_validate), 全部 print 文本表格 + 中文标签 + shorten() 对齐 + cmd_schedule_delete 仿 cmd_session_delete 走 input() 确认 (EOFError 容错)
- **依赖**: T3 (需要 ScheduleStore)
- **关联测试任务**: T6

### Task 6: tests/unit/ — schedule_cli 单测 [type=test]
- **涉及目录**: `tests/unit/`
- **涉及文件**:
  - `tests/unit/test_schedule_cli.py` (新建, ~200 行)
- **描述**: 7 子命令 happy + error path (空 list / name 未找到 / name 重复 / schedule 字段不全 / to_email 缺 @); run-now 调 send_reply 验证; validate 输出格式验证
- **验证方法**:
  - `pytest tests/unit/test_schedule_cli.py -v` — 期望 14+ passed (happy + error)
  - `pytest tests/unit/ -q` — 期望全过
  - `ruff check mailcode/schedule_cli.py tests/unit/test_schedule_cli.py` — 期望 All checks passed
- **关联 impl**: T5

### Task 7: mailcode/ + mailcode/resources/ — schedule 配置段 [type=impl]
- **涉及目录**: `mailcode/`, `mailcode/resources/`
- **涉及文件**:
  - `mailcode/config.py` (修改, 加 SCHEDULE_DEFAULTS dict + get_schedule_config() + validate_serve_config 加 schedule 可选校验)
  - `mailcode/resources/default.json` (修改, 顶层加 schedule 段, 含 _notes 注释 + enabled/tick_seconds=30/max_concurrent=1)
- **描述**: 加 SCHEDULE_DEFAULTS 仿 SESSION_DEFAULTS 模式; get_schedule_config 仿 get_session_config 模式; validate_serve_config 加 schedule 段可选校验 (warn 而非阻塞, 允许空); default.json 加 schedule 段
- **关联测试任务**: T6 (schedule_cli 单测会隐式覆盖配置加载)

### Task 8: mailcode/cli + mailcode/server/ — CLI + serve 集成 [type=impl]
- **涉及目录**: `mailcode/`
- **涉及文件**:
  - `mailcode/cli.py` (修改, 加 p_schedule 子解析器 + 7 子命令参数解析 + cmd_schedule 路由 + main 加 elif args.command == "schedule")
  - `mailcode/server.py` (修改, run_serve 改造: 实例化 Scheduler(email_channel=listener.email_channel), signal_handler 同步停两个, try/finally scheduler.stop() + scheduler.join(timeout=10), --once 模式不启 scheduler)
- **描述**: CLI 集成 7 子命令 (含 --type/--interval-seconds/--time/--day-of-week/--day-of-month/--prompt/--to-email/--cwd/--subject-prefix 参数); serve 启动 scheduler 线程, 共用 listener.email_channel 单例
- **依赖**: T3, T5, T7
- **关联测试任务**: T9 (手动验收)

### Task 9: 整体 review + 实施报告 [type=test]
- **涉及目录**: `tests/`, `mailcode/`, `docs/`
- **涉及文件**:
  - `docs/reports/2026-06-13-schedule-report.md` (新建, 实施报告)
- **描述**: 全量测试套件 + lint 检查 + 写实施报告; MANUAL_ACK_REQUIRED 项原样保留供 Phase 4 用户勾选
- **验证方法**:
  - `pytest tests/unit/ -q` — 期望全过
  - `ruff check mailcode/ tests/` — 期望 All checks passed
  - 写 `docs/reports/2026-06-13-schedule-report.md`
- **关联 impl**: T1-T8
- **MANUAL_ACK_REQUIRED** (Phase 4 报告须原样保留, 由用户勾选):
  - [ ] 加 60s interval 任务, 启动 serve 后 60s 内收到邮件
  - [ ] `mailcode schedule run-now <name>` 不开 serve 也能触发, 收件箱立刻收到邮件
  - [ ] Ctrl-C 后 scheduler 优雅退出, relay.log 出现 "Scheduler stopped"
  - [ ] daily/weekly/monthly 跨边界行为符合预期 (2 月没 31 号跳过到 3 月)
  - [ ] `mailcode schedule list` / `show` / `add` / `enable` / `disable` / `delete` / `validate` 输出格式符合设计

## 验证清单

- [ ] **T1+T2、T3+T4、T5+T6、T7(配对 T6)、T8(配对 T9)** 配对均已 completed
- [ ] 运行 `pytest tests/unit/ -q` — 全过
- [ ] 运行 `ruff check mailcode/ tests/` — All checks passed
- [ ] 运行 `mailcode schedule list --help` 等 7 子命令 --help — 输出符合设计
- [ ] T9 的 MANUAL_ACK_REQUIRED 项已由用户在 Phase 4 报告确认